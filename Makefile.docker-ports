# Show Docker service ports
docker-ports:
	@echo "$(BLUE)PyLama Docker Services Port Mappings:$(NC)"
	@echo "$(YELLOW)━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━$(NC)"
	@echo "$(GREEN)Service\t\tContainer Port\tHost Port\tURL$(NC)"
	@echo "$(YELLOW)━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━$(NC)"
	@echo "$(GREEN)WebLama\t\t80\t\t9081\t\thttp://localhost:9081$(NC)"
	@echo "$(GREEN)APILama\t\t8080\t\t9080\t\thttp://localhost:9080$(NC)"
	@echo "$(GREEN)PyLama\t\t8003\t\t9003\t\thttp://localhost:9003$(NC)"
	@echo "$(GREEN)PyBox\t\t8000\t\t9000\t\thttp://localhost:9000$(NC)"
	@echo "$(GREEN)PyLLM\t\t8001\t\t9001\t\thttp://localhost:9001$(NC)"
	@echo "$(GREEN)SheLLama\t8002\t\t9002\t\thttp://localhost:9002$(NC)"
	@echo "$(GREEN)Ollama\t\t11434\t\t11435\t\thttp://localhost:11435$(NC)"
	@echo "$(GREEN)LogLama\t\t5001\t\t6001\t\thttp://localhost:6001$(NC)"
	@echo "$(YELLOW)━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━$(NC)"
	@echo "\n$(BLUE)To check container status:$(NC)"
	@echo "$(YELLOW)  docker-compose ps$(NC)"
	@echo "\n$(BLUE)To view logs for a specific service:$(NC)"
	@echo "$(YELLOW)  docker-compose logs -f <service-name>$(NC)"
	@echo "  Example: docker-compose logs -f weblama"
